//! VectorHawk runner — core app logic.
//!
//! Houses state bootstrap, installer, importer, validator, resolver, policy,
//! executor, registry HTTP client, auth, and updater. Ported from
//! `skillrunner-core` with the app-support directory renamed from
//! `SkillClub/SkillRunner/` to `VectorHawk/`.

#[doc(hidden)]
pub fn _placeholder() {}
