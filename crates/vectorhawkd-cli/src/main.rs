//! `vectorhawk` — VectorHawk runner user CLI.
//!
//! User-facing commands: `skill`, `mcp`, `daemon`, `doctor`, `auth`.
//! `vectorhawk mcp serve` is the AI-client entry point — it execs the shim
//! relay logic in this same process (or in `vectorhawkd-shim` if extracted).

fn main() {
    eprintln!("vectorhawk: not yet implemented (M0 placeholder)");
    std::process::exit(2);
}
